{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  },
  "scripts": {
  "dev": "turbo run dev",
  "build": "turbo run build",
  "lint": "turbo run lint"
}

}
